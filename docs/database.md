# Banco de Dados

- Conexão via `src/database/Connect.php`
- Configuração em `settings.php` ou `.env`
- Migrações e comandos em `db/commands/`
- Operações CRUD via classes `Create`, `Read`, `Update`, `Delete` ou pelos métodos dos Models 